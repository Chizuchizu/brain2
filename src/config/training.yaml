inference: False
base:
  n_folds: 4
  num_boost_round: 3000
  seed: 1234
  submit: true
  optuna: false
  time_budget: 60 * 10

parameters:
  objective: "regression"
  metric: "rmse"
  num_leaves: 31
  max_depth: 7
  learning_rate: .07

  # feature_fraction: 0.5
  verbose: -1