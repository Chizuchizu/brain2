inference: False
base:
  n_folds: 4
  num_boost_round: 2000
  seed: 1234
  submit: true
  optuna: false
  time_budget: 60 * 10

parameters:
  objective: "regression"
  metric: "rmse"
  num_leaves: 16
  max_depth: 7
  learning_rate: .05
  # feature_fraction: 0.5
  verbose: -1