inference: False
base:
  n_folds: 4
  num_boost_round: 2000
  seed: 1234
  submit: true

parameters:
  objective: "regression"
  metric: "rmse"
  num_leaves: 31
  max_depth: 6
  learning_rate: .03
  verbose: -1